spring:
  application:
    name: spring-config-client
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8888
      label: master
      username: root
      password: root